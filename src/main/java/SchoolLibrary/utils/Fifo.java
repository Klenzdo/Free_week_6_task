package SchoolLibrary.utils;

import SchoolLibrary.model.Person;

import java.util.Comparator;

public class Fifo implements Comparator<Person> {

    @Override
    public int compare(Person o1, Person o2) {
        return 0;
    }
}
